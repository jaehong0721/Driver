<resources>

    <style name="DriverTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/primaryGreen</item>
        <item name="colorPrimaryDark">@color/primaryGreen</item>
        <item name="colorAccent">@color/primaryYellow</item>

        <item name="android:textColor">@color/textBlack</item>

        <item name="colorControlActivated">@color/primaryGreen</item>
        <item name="colorControlNormal">@color/primaryGreen</item>
    </style>

    <style name="TextAppearance.FontPath" parent="android:TextAppearance">
        <item name="fontPath">fonts/dohyun.ttf</item>
    </style>

</resources>
